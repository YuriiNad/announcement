// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
	production: false,
	firebase: {
		apiKey: "AIzaSyA5jkzx-qimPiUK4J6sfex3JNQ8K4pmsZE",
		authDomain: "announcement-task.firebaseapp.com",
		projectId: "announcement-task",
		storageBucket: "announcement-task.appspot.com",
		messagingSenderId: "223092801936",
		appId: "1:223092801936:web:4402f315a69d0ffb527315",
		databaseURL: "https://announcement-task-default-rtdb.europe-west1.firebasedatabase.app",
	}
}

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
